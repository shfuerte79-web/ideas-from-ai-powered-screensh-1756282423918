import "./globals.css";

export const metadata = {
  title: "Ideas from: AI-Powered Screenshot to Text",
  description: "Revolutionary AI solution based on Ideas from: AI-Powered Screenshot to Text",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}